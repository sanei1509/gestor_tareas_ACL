spring.application.name=gestor_tareas_ACL


#apuntamos a nuestra base de datos en el contenedor
# Configuración del DataSource
spring.datasource.url=jdbc:mysql://localhost:5432/${DB_NAME}?allowPublicKeyRetrieval=true&useSSL=false&serverTimezone=UTC
spring.datasource.username=${DB_USERNAME}
spring.datasource.password=${DB_PASSWORD}
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

